# Claude Code Instructions

## After Code Changes

- After modifying code and before asking the user to verify, compile the project with `npm run compile` so the user can refresh VS Code to test the changes.
